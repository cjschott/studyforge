# Generate Multi-Select Questions

Generate multi-select questions only when multiple correct answers are meaningfully required.

Rules:

- `answer` must be an array.
- Avoid "all of the above" and clue-heavy wording.
- Keep distractors in the same category as correct choices.
- Include lineage and keep status as `generated`.
